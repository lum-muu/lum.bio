name = "lum-bio"
pages_build_output_dir = "dist"

[vars]
# Default to same-origin route handled by a Cloudflare Email Worker (or Pages Function).
VITE_CONTACT_ENDPOINT = "/api/contact"
# Optional overrides
VITE_CONTACT_TIMEOUT = "10000"
VITE_APP_ENV = "production"
